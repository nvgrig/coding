package com.nvgrig.coding.leet;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class CompressTest {
    @Test
    void test() {
        Compress compress = new Compress();
        compress.compress(new char[]{'a','a','b','b','c','c','c'});
    }

}